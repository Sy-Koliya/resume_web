# Optional service environment values belong here.
# DeepSeek is configured in /etc/bite-hunt/deepseek.json instead.
# BITE_HUNT_AI_API_KEY remains supported only for older installations.

BITE_HUNT_AI_API_KEY=
